<!DOCTYPE html>
<html lang="km">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ផ្ទះបាយ - កម្មង់ថ្មីៗ</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="/socket.io/socket.io.js"></script>
</head>
<body>
  <div class="container my-5">
    <h1 class="text-center mb-4">កម្មង់កំពុងរង់ចាំធ្វើ</h1>

    <% if (orders.length === 0) { %>
      <p class="text-center">មិនទាន់មានកម្មង់ថ្មីទេ...</p>
    <% } else { %>
      <% orders.forEach(order => { %>
        <div class="card mb-3">
          <div class="card-body">
            <h5 class="card-title">តុលេខ: <%= order.table %></h5>
            <p class="card-text">
              <strong>ម្ហូប៖</strong> <%= order.items.join(', ') %>
            </p>
            <p class="text-muted">ពេលវេលា៖ <%= new Date(order.timestamp).toLocaleString('km-KH') %></p>
            <form action="/kitchen/done/<%= order._id %>" method="POST">
              <button type="submit" class="btn btn-success">ធ្វើរួចរាល់</button>
            </form>
          </div>
        </div>
      <% }) %>
    <% } %>
  </div>

  <script>
    const socket = io();
    socket.on('newOrder', () => location.reload());
    socket.on('orderUpdated', () => location.reload());
  </script>
</body>
</html>