<!DOCTYPE html>
<html lang="km">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>កម្មង់អាហារ - តុ <%= table %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container my-5">
    <h1 class="text-center mb-4">ម៉ឺនុយអាហារ - តុ <%= table %></h1>
    <form action="/order" method="POST">
      <input type="hidden" name="table" value="<%= table %>">
      
      <div class="mb-3">
        <h4>ជ្រើសរើសម្ហូប</h4>
        <div class="form-check">
          <input class="form-check-input" type="checkbox" name="items" value="ភីហ្សា">
          <label class="form-check-label">ភីហ្សា</label>
        </div>
        <div class="form-check">
          <input class="form-check-input" type="checkbox" name="items" value="ប៊ឺហ្គឺ">
          <label class="form-check-label">ប៊ឺហ្គឺ</label>
        </div>
        <div class="form-check">
          <input class="form-check-input" type="checkbox" name="items" value="មីកុឡា">
          <label class="form-check-label">មីកុឡា</label>
        </div>
        <!-- បន្ថែមម្ហូបផ្សេងទៀតតាមតម្រូវការ -->
      </div>

      <button type="submit" class="btn btn-success btn-lg w-100">ផ្ញើកម្មង់</button>
    </form>
  </div>
</body>
</html>