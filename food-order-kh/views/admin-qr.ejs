<!DOCTYPE html>
<html lang="km">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>បង្កើត QR Code សម្រាប់តុ</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .qr-container { text-align: center; margin: 20px; }
    .qr-img { max-width: 300px; }
  </style>
</head>
<body class="bg-light">
  <div class="container my-5">
    <h1 class="text-center mb-4">បង្កើត QR Code សម្រាប់តុ</h1>

    <form action="/admin/qr" method="POST" class="mb-5">
      <div class="row g-3 justify-content-center">
        <div class="col-md-4">
          <label for="tableNumber" class="form-label">លេខតុ</label>
          <input type="number" class="form-control" id="tableNumber" name="table" placeholder="ឧ. 300" required min="1">
        </div>
        <div class="col-md-2 d-flex align-items-end">
          <button type="submit" class="btn btn-primary w-100">បង្កើត QR Code</button>
        </div>
      </div>
    </form>

    <% if (qrCode) { %>
      <div class="qr-container card p-4 shadow">
        <h4>QR Code សម្រាប់តុលេខ <%= table %></h4>
        <p class="text-muted">ស្កេនដើម្បីបើកម៉ឺនុយ</p>
        <img src="<%= qrCode %>" alt="QR Code for Table <%= table %>" class="qr-img img-fluid mb-3">
        <a href="<%= qrCode %>" download="qr-table-<%= table %>.png" class="btn btn-success">ទាញយក QR Code</a>
      </div>
    <% } %>
  </div>
</body>
</html>